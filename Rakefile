require 'rake'

desc 'Move dotfiles into standard positions'
task :install do
  symlinks = Dir.glob('*/**(.symlink)')

  skip_all      = false
  overwrite_all = false
  backup_all    = false

  symlinks.each do |link|
    puts symlink.to_s
  end
end
